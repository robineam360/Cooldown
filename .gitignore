# Gradle
.gradle/
build/
app/build/

# Local machine config (contains SDK paths — never commit)
local.properties

# IDE
.idea/
*.iml
.kotlin/

# macOS
.DS_Store

# Claude Code local session settings
.claude/settings.local.json

# Release signing — NEVER commit these (losing them means you can't update the app)
keystore.properties
*.jks
*.keystore

# Built APK — distributed via GitHub Releases, not committed to the repo
*.apk

# Private handover notes — never commit
HANDOVER.md
